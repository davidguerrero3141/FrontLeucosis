export class Coordinate{
  id: number
  latitude: number
  length: number

  constructor(id: number, latitude: number, length: number){

    this.id = id
    this.latitude = latitude
    this.length = length
  }
}
